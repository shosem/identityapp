<h1 class="border-b border-black h-10 pt-2">投稿一覧</h1>
<br>

<div id="posts" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
  <% @posts.each do |post| %>
    <%= render post %>  <!-- _post.html.erb のカードがここに入る -->
  <% end %>
</div>

<% if current_user.present? %>
  <%= link_to "新規作成", new_post_path, class: "inline-flex items-center px-6 py-3 bg-gray-200 text-gray-800 font-medium rounded-lg hover:bg-gray-300 transition" %>
<% else %>
  <p class="mt-8">ログイン後、投稿・閲覧できるようになります</p>
  <%= link_to "ログイン", login_path, class: "inline-flex items-center px-6 py-3 bg-gray-200 text-gray-800 font-medium rounded-lg hover:bg-gray-300 transition" %>
<% end %>